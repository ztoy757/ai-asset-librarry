# AI生成物ライブラリ 要件定義書

2026-09-26 · Toy

## 1. 目的と背景

AIで生成した画像・音楽・動画を保管・整理する個人用ライブラリを作り、同時に仕事で抱えているテストと自動化の課題を検証する。

- **個人の困りごと**：生成物でPCの容量が圧迫され、保存先にも困っている
- **検証台**：外部結合テストの重さと偽の緑、特性テストを使ったリファクタリング、AIDLCによる機能追加・改修・脆弱性対応の自動化を、社内の制約なしに試す
- **機能追加の練習**：何回か機能追加を重ねて、テストと自動化の仕組みが効くかを確かめられる題材にする

## 2. スコープ

利用者は自分一人。アプリは小さく保ち、テスト・インフラ・CIの仕組みを厚くする。

**やること**

- 生成物のアップロード、一覧、メタデータ管理、整理ルール
- 3層のテスト、Terraform、GitHub ActionsによるCIとDependabot

**やらないこと（当面）**

- 費用が発生するAzureリソースの利用と実デプロイ
- 生成物そのものの生成（外部のAIサービスで作ったものを取り込むだけ）

## 3. 機能要件（リリース計画）

v1で最小の保管機能を作り、v2以降でルールを段階的に足していく。各バージョンが1回分の機能追加の検証になる。

| 版 | 機能 | 検証の狙い |
| --- | --- | --- |
| v1 | 画像・音楽・動画のアップロードと一覧表示。本体はBlob、メタデータはMySQLに保存 | ロジックをあえて画面側に書き、後のリファクタリング練習の素材にする |
| v2 | 生成時のメタデータ記録（プロンプト、モデル名、シード値、パラメータ） | スキーマ変更を伴う機能追加 |
| v3 | お気に入り・評価、タグ付け | 画面と検索条件の追加 |
| v4 | 整理ルール（評価なしで30日経過したら削除候補、同じシードとプロンプトの重複検出） | ドメインロジックの追加と単体テスト |
| v5 | 保存先の階層化（Hot / Cool / Archiveへの自動移動） | Blobのライフサイクル管理をTerraformで表現 |
| v6 | 生成サービスごとの利用規約管理（商用利用の可否など） | ルール変更に強い設計かの確認 |

## 4. 技術構成と非機能要件

仕事と同じ構成にそろえ、得た知見をそのまま持ち込めるようにする。

| 領域 | 採用技術 |
| --- | --- |
| アプリ | Java / Jakarta EE（WAR）、WildFly |
| ビルド | Maven |
| メタデータ | MySQL |
| ファイル本体 | Azure Blob（ローカルではAzurite） |
| テスト | JUnit、Testcontainers、Playwright、ApprovalTests |
| インフラ | Terraform |
| CI / セキュリティ | GitHub Actions、Trivy、Dependabot |

**リポジトリ構成**

```
ai-asset-library/
├── app/          # Jakarta EE（WAR）
├── e2e/          # Playwright + Testcontainers
├── infra/
│   ├── azure/    # azurerm（静的チェックとtestのみ）
│   └── local/    # dockerプロバイダー
└── .github/      # workflows, dependabot.yml
```

**非機能要件**

- 費用ゼロで開発・検証できること
- テストが偽の緑にならないこと（エラー画面や5xxで必ず失敗する）
- テストが遅すぎて回されなくなることがないこと

## 5. テスト戦略

単体・結合・シナリオの3層で、画面を通すテストは主要フローの少数に絞る。

| 層 | 対象 | 手段 | 量 |
| --- | --- | --- | --- |
| 単体 | ドメインロジック（整理ルールなど） | JUnit | 一番多い |
| 結合 | リポジトリ、Blobの読み書き | Testcontainers（MySQL、Azurite） | 中くらい |
| シナリオ | 主要な業務フロー | Playwright + Testcontainers（WildFly） | 少数 |

**シナリオテストの方針**

- コンテナはシングルトンで1回だけ起動し、全テストで共有する
- エラー画面や5xxを検知したら即失敗させる共通処理を入れる
- デフォルトのタイムアウトを短くし、固定sleepは使わない

**特性テストとリファクタリング**

- v1で画面に書いたロジックは、特性テスト（ApprovalTests）で今の挙動を固定してから移す
- 固定する出力は、業務上意味のある項目の構造化テキストと、操作後のDBの状態
- 特性テストには `@Tag("characterization")` を付け、恒久的なテストと区別する
- 進め方：画面テストで守る → ロジックをドメインへ移す → 単体テストで守り直す → 画面テストを減らす

## 6. インフラとCI/CD

Terraformは無料の範囲で検証し、Azureへの実デプロイは必要になるまで行わない。

**Terraform**

- `infra/azure`：静的チェック（fmt、validate、tflint、Trivyの設定スキャン）と、mock_providerを使った `terraform test`
- `infra/local`：dockerプロバイダーで、WildFly・MySQL・Azuriteをローカルに構築する
- 実デプロイを試す場合は、Azureの無料アカウントで短期間だけ行い、予算アラートと `terraform destroy` を徹底する

**GitHub Actions**

- `mvn verify`（3層のテスト）
- Terraformの静的チェックと `terraform test`
- Trivy（コンテナイメージとTerraformの設定）

**Dependabot**

- Maven、Docker、GitHub Actions、Terraformの依存を監視する
- 依存更新のPRで主要フローのシナリオテストが回ることを確認する

**効果の記録**

- テスト全体の実行時間（改善前と改善後）
- テストが捕まえた問題の件数（特にDependabotのPR）

## 7. 前提・未決事項

- [ ] アプリサーバーはWildFlyで確定か（この文書はWildFlyを前提に記載）
- [ ] GitHubの公開リポジトリにするか（Zenn・Qiitaの記事にするなら公開が楽）
- [ ] 認証を入れるか（利用者が自分一人なら不要の可能性）
- [ ] 実データの保存先をどうするか（外付けHDD・NAS、将来的にAzure BlobのCool / Archive層）
